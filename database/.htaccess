# Scripts SQL (incluye seed_prod.sql con el hash del admin):
# NUNCA deben ser accesibles ni descargables por web.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
